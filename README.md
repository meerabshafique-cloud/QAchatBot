# QAchatBot

A simple to-do list task manager (Node.js + Express, JSON file storage).

## Setup

```bash
npm install
```

## Run

```bash
npm start
```

Then visit http://localhost:3000 in your browser.

Tasks are persisted to `data/tasks.json`.
